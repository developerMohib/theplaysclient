import Loader from '@/components/common/LoadingInit';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Loader variant="wave" />
        </div>
    );
};

export default loading;